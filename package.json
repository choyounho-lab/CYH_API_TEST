{
  "name": "cyh-api-test-worker",
  "private": true,
  "scripts": {
    "build": "npm --prefix frontend ci && npm --prefix frontend run build",
    "deploy": "npm run build && npx wrangler pages deploy frontend/dist --project-name=cyh-api-test"
  }
}
